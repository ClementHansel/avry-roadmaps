version: '3.8'

services:
  avry_roadmap:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: avry-roadmap
    ports:
      - "8084:8084"
    environment:
      # Database
      DATABASE_URL: ${DATABASE_URL}
      
      # Service
      PORT: 8084
      ENVIRONMENT: ${ENVIRONMENT:-development}
      
      # JWT
      JWT_SECRET: ${JWT_SECRET}
      
      # API Keys (as needed)
      # API_KEY: ${API_KEY}
    
    restart: unless-stopped
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8084/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
